# AGENTS.md — packages/subagent

> Auto-generated context for coding agents. Last updated: 2026-05-17

## Purpose

Stub package — `packages/subagent/src/` is currently empty. This package exists as a placeholder for future subagent functionality.

## Status

**EMPTY** — No source files in `src/`. Package is structured but not implemented.

## Planned Architecture

Based on the monorepo context (pi-coding-agent wrapper, workspace indexing, MCP integration), this package likely intended to:

- Provide subagent orchestration capabilities
- Manage agent spawning and lifecycle
- Coordinate between pi-sdk and workspace features

## Dependencies

This package references no other `@lamda/*` packages yet (no imports in `src/`).

## Action Required

This package needs implementation. The `apps/web/src/features/subagent/` stub feature references this package and will need the public API to be defined here.

## Related

- [packages/pi-sdk](../pi-sdk/AGENTS.md) — Pi coding agent wrapper
- [apps/web/src/features/subagent](../../apps/web/src/features/subagent/) — Web UI stub for subagent feature