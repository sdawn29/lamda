// Shared content-view selector for the review panel, used by the toolbar,
// the source-control content area, and the main ReviewPanel header.
export type ContentView = "turn" | "all" | "history" | "github" | "gitlab"
